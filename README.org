* Personal Emacs config

Literate, component-based Emacs (30+).


** Layout
- =early-init.el= :: pre-frame: GC, disable =package.el= startup, theme/frame cache.
- =init.el= :: Elpaca bootstrap, tangle/byte-compile, load =config=.
- =Makefile= :: =make compile= / =make clean= for generated config.
- =config.org= :: always tangled: core settings, sessions/desktop, GnuPG, =kwarks/*= helpers.
- =components.org= :: manifest of optional features and languages.
- =components/<name>.org= :: one file per component.
- =lisp/ext/= :: custom elisp, autoloaded on demand.
- =lisp/private/= :: optional private extensions (git-ignored).
- =fonts/= :: JetBrains Mono.
- =elpa/= :: vendored ELPA packages.
- =githooks/= :: commit-msg hook.

** Adding a component
1. Add an entry to =components.org= under the right category.
2. Create =components/foo.org=.
3. Flip =[ ]= to =[X]= and restart.

** Rebuilding manually
#+begin_src sh
make compile
make clean
#+end_src
